import { Layers, LucideIconData, ShieldCheck, Lock, UserCog, Users } from 'lucide-angular';

export const ICON_MAP: Record<string, LucideIconData> = {
  'shield-check': ShieldCheck,
  lock: Lock,
  layers: Layers,
  'user-cog': UserCog,
  users: Users,
};
